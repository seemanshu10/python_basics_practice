notes = ['Fix lighting on shot 03', 'Add motion blur', 'Color grade pass needed']

for i in range(len(notes)):
    print(i, notes[i])


# Output:
# 0 Fix lighting on shot 03
# 1 Add motion blur
# 2 Color grade pass needed